<div class="row">
  <div class="col-md-10 col-md-offset-1">


  <div class="col-xs-10">

    <div class="row">
<% @items.each do |item| %>

  <div class="col-xs-4 product-info">
    <%= link_to customer_item_path(item) do %>
      <%= attachment_image_tag item, :image, :fill, 190, 150, fallback: "no_image.jpg" %></br>
      <h4><%= item.name %></h4>
      ¥<%= item.price %>
    <% end %>
  </div>

<% end %>
</div>
  </div>

  </div>
</div>